const express=require('express'); const router=express.Router(); const db=require('../db'); router.get('/',(req,res)=>{ res.json(db.prepare('SELECT * FROM suppliers').all()) }); router.post('/',(req,res)=>{ const {name,contact}=req.body; const info=db.prepare('INSERT INTO suppliers (name,contact) VALUES (?,?)').run(name,contact); res.json(db.prepare('SELECT * FROM suppliers WHERE id=?').get(info.lastInsertRowid)); }); module.exports=router;
